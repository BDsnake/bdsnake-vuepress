---
title: 设计模式
createTime: 2024/10/28 16:11:55
permalink: /article/vfvbcby7/
---


# 设计模式

## 创建型模式

### 工厂模式

静态工厂

~~~java
/**
 * @author BDsnake
 * @since 2023/9/5 18:16
 */
public class TestStaticFactory {
    public static void main(String[] args) {
        Employee e1 = TestStaticFactory.getEmployee();
        Employee e2 = TestStaticFactory.getEmployee();
        System.out.println(e1);
        System.out.println(e2);
    }
    public static Employee getEmployee(){
        return new Employee();
    }
}
class Employee{
    String name;
    int age;
}

~~~

### 抽象工厂

要用到的：

抽象工厂-抽象工厂实现

抽象产品-抽象产品的实现

![image-20230905185304971](https://bd-hexo.oss-cn-beijing.aliyuncs.com/img/image-20230905185304971.png)

**举个例**

1、定义一个抽象产品
定义抽象产品，能够提供功能->查询链上操作记录&查询币种

~~~java
//获取链上操作记录的工厂
public interface ChainActionsFinder {
    String getChainActions(String url);
    String getTokenType(String url);
}
~~~

2、定义一个抽象工厂
定义抽象工厂，能够获取产品->链上资产查询器

```
public interface ChainActionsFinderFactory {
    ChainActionsFinder getFinder();
}
```

3、定义具体产品

这里有两家api服务公司，debank和blockin

~~~java
public class DebankChainActionFinder implements ChainActionsFinder{
    @Override
    public String getChainActions(String url) {
        return "debankChainActions"+url;
    }

    @Override
    public String getTokenType(String url) {
        return "debankTokenType"+url;
    }
}

~~~

~~~java
public class BlockinChainActionFinder implements ChainActionsFinder{
    @Override
    public String getChainActions(String url) {
        return "blockinChainActions"+url;
    }

    @Override
    public String getTokenType(String url) {
        return "blockinChainActions"+url;
    }
}
~~~

4、定义具体工厂
这里有两家api服务公司，debank和blockin

~~~java
public class DebankChainActionsFinderFactory implements ChainActionsFinderFactory{
    @Override
    public ChainActionsFinder getFinder() {
        return new DebankChainActionFinder();
    }
}

~~~

~~~java
public class BlockinChainActionsFinderFactory implements ChainActionsFinderFactory{
    @Override
    public ChainActionsFinder getFinder() {
        return new BlockinChainActionFinder();
    }
}

~~~

工厂模式本身是为了屏蔽构建对象的细节，以实现面向接口的目的，以达到无感知切换产品的目的。到了抽象工厂，升级成了连工厂都是多实现的。不同的工厂里有对同一个产品的不同实现。当我们想要切换使用的产品的时候，直接切换工厂就行了，而不用在普通工厂里去修改代码。把工厂都抽象出来，是真正实现了面向接口编程。符合了开闭原则

简单来说，现在我们只需要在业务代码中创建工厂时，来决定用哪一种工厂

### 建造者/生成器模式（Builder）

Builder模式是为了创建一个复杂的对象，需要多个步骤完成创建，或者需要多个零件组装的场景，且创建过程中可以灵活调用不同的步骤或组件。

### 原型

原型模式是根据一个现有对象实例复制出一个新的实例，复制出的类型和属性与原实例相同。

应用原型模式的实例：

数组的拷贝

~~~java
// 原型:
String[] original = { "Apple", "Pear", "Banana" };
// 新对象:
String[] copy = Arrays.copyOf(original, original.length);
~~~

对于普通类，Object类中提供乐一个clone方法，实现CLoneable接口来标识一个对象可复制

```
public Object clone()
```

但是它返回的是Object，拿到之后一般还需要我们强转

所以更好的方法是自己写一个copy方法

~~~java
public class Student {
    private int id;
    private String name;
    private int score;

    public Student copy() {
        Student std = new Student();
        std.id = this.id;
        std.name = this.name;
        std.score = this.score;
        return std;
    }
}
~~~



### 单例

Singleton模式是为了保证一个程序的运行期间，某个类有且只有一个全局唯一实例；

Singleton模式既可以严格实现，也可以以约定的方式把普通类视作单例。

下面介绍几种实现

#### 饿汉式

1. 只有`private`构造方法，确保外部无法实例化；
2. 通过`private static`变量持有唯一实例，保证全局唯一性；
3. 通过`public static`方法返回此唯一实例，使外部调用方能获取到实例。

举例：

~~~java
public class TestSingleton {
    //单例模式-饿汉式
    private static final TestSingleton INSTANCE = new TestSingleton();
    //通过静态方法返回实例
    public static TestSingleton getInstance(){
        return INSTANCE;
    }
    //设置默认构造方法为私有，防止创建新的实例
    private TestSingleton() {
    }

    public static void main(String[] args) {
        TestSingleton t1 = TestSingleton.getInstance();
        TestSingleton t2 = TestSingleton.getInstance();
        System.out.println(t1);
        System.out.println(t2);
    }
}
~~~

优点：这种写法比较简单，就是在类装载的时候就完成实例化。避免了线程同步问题。
缺点：在类装载的时候就完成实例化，没有达到Lazy Loading的效果。如果从始至终从未使用过这个实例，则会造成内存的浪费。

但是！！！

高版本的JVM加载class并没有初始化静态变量，直到第一次调用getInstance()才会执行createInstance()方法，也就是JVM本身就是懒加载static变量的
所以实际用的时候直接用饿汉式就行了，JVM已经完成优化了

可以试一下

~~~java
public class Singleton {

    static {
        System.out.println("init Singleton class...");
    }

    private static Singleton instance = createInstance();

    private static Singleton createInstance() {
        System.out.println("create singleton instance...");
        return new Singleton();
    }

    private Singleton() {
    }

    public static Singleton getInstance() {
        return instance;
    }
}
~~~

~~~java
Singleton s = null;
System.out.println(Singleton.class);
s = Singleton.getInstance(); // create singleton instance...
System.out.println(s);
~~~



#### 懒汉式

这种写法起到了Lazy Loading的效果，但是只能在单线程下使用。如果在多线程下，一个线程进入了if (singleton == null)判断语句块，还未来得及往下执行，另一个线程也通过了这个判断语句，这时便会产生多个实例。所以在多线程环境下不可使用这种方式。

~~~java
/**
 * @author BDsnake
 * @since 2023/9/6 14:27
 */
public class TestLazySin {
    private static TestLazySin INSTANCE;
    private TestLazySin(){};
    public static TestLazySin getInstance() {
        if (INSTANCE == null){
            System.out.println("加载实例");
            INSTANCE = new TestLazySin();
        }
        return INSTANCE;
    }

    public static void main(String[] args) {
        TestLazySin t1 = TestLazySin.getInstance();
        TestLazySin t2 = TestLazySin.getInstance();
        System.out.println(t1);
        System.out.println(t2);
    }
}

~~~

#### 枚举

不仅能避免多线程同步问题，而且还能防止反序列化重新创建新的对象。

~~~java
public enum World {
    // 唯一枚举:
	INSTANCE;

	private String name = "world";

	public String getName() {
		return this.name;
	}

	public void setName(String name) {
		this.name = name;
	}
}
~~~



## 结构性模式

### 适配器

*将一个类的接口转换成客户希望的另外一个接口，使得原本由于接口不兼容而不能一起工作的那些类可以一起工作。*

Adapter模式可以将一个A接口转换为B接口，使得新的对象符合B接口规范。

编写Adapter实际上就是编写一个实现了B接口，并且内部持有A接口的类：

```
public BAdapter implements B {
    private A a;
    public BAdapter(A a) {
        this.a = a;
    }
    public void b() {
        a.a();
    }
}
```

在Adapter内部将B接口的调用“转换”为对A接口的调用。

只有A、B接口均为抽象接口时，才能非常简单地实现Adapter模式。

比如：

```
String[] exist = new String[] {"Good", "morning", "Bob", "and", "Alice"};
Set<String> set = new HashSet<>(Arrays.asList(exist));
```

将String数组转换为了List

#### 举例

假如我们原来有一个Task类，实现了Callable接口

~~~java
package adapter;

import java.util.concurrent.Callable;

/**
 * @author BDsnake
 * @since 2023/9/6 14:40
 */
public class Task implements Callable<Long> {
    private long num;
    public Task(long num){
        this.num = num;
    }
    @Override
    public Long call() throws Exception {
        long r =0;
        for(long n = 1;n<=this.num;n++){
            r=r+n;
        }
        System.out.println("res:"+r);
        return r;
    }
}

~~~

现在我们需要用一个thread去执行它

~~~java
Callable<Long> callable = new Task(123450000L);
        Thread thread = new Thread(callable);
        thread.start();
~~~

现编译不过！因为`Thread`接收`Runnable`接口，但不接收`Callable`接口，肿么办？

一个办法是改写`Task`类，把实现的`Callable`改为`Runnable`，但这样做不好，因为`Task`很可能在其他地方作为`Callable`被引用，改写`Task`的接口，会导致其他正常工作的代码无法编译。

另一个办法不用改写`Task`类，而是用一个Adapter，把这个`Callable`接口“变成”`Runnable`接口，这样，就可以正常编译：这个Adapter就是适配器

~~~java
public class RunnableAdapter implements Runnable{
    private Callable<?> callable;
    RunnableAdapter(Callable<?> callable){
        this.callable = callable;
    }
    @Override
    public void run() {
        try {
            callable.call();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
~~~

然后这样调用就行了

~~~java
public class Main {
    public static void main(String[] args) {
        Callable<Long> callable = new Task(123450000L);
        Thread thread = new Thread(new RunnableAdapter(callable));
        thread.start();
    }
}
~~~



## 行为型模式